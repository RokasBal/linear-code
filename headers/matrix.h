#ifndef MATRIX_H
#define MATRIX_H

// File that defines vector and matrix types used in the program

#include <vector>
#include <cstdint>

using Matrix = std::vector<std::vector<uint8_t>>;
using Vec = std::vector<uint8_t>;

#endif // MATRIX_H